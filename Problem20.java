package euler;

import java.math.BigInteger;

public class Problem20 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		System.out.print(numberSum(shortFactorial(20)));
	}
	public static long multiplyAndShorten(BigInteger number1, BigInteger number2)
	{
		
		BigInteger n = number1 * number2;
		System.out.print(n + "\n");
		if(n % 10 == 0)
		{
			return n / 10;
		}
		else
		{
			return n;
		}
	}
	public static BigInteger shortFactorial(int number)
	{
		BigInteger n = 1;
		for(int i = number; i > 0; i--)
		{
			n = multiplyAndShorten(n, i);
		}
		return n;
	}
	public static BigInteger numberSum(BigInteger number)
	{
		BigInteger sum = 0;
		while (number > 0) {
			sum = sum + number % 10;
			number = number / 10;
		}
		return sum;
		*/
	}
}
